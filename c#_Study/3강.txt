3강


int number1 = Convert.ToInt32(sum1.Text);
Convert를 사용한 Int형변환
String을 제외한 모든 데이터타입은 ToString이라는 메서드를 가지고있다.
어디든지 화면에 출력할때는 String으로 사용된다.
그만큼 바뀔일이 많다.

함수 = Function
함수 = 메서드

데이터타입을 가진 함수는 인자를 받고 값을 리턴해줘야한다. 이것이 기본
void는 리턴값이 없다.

함수는 호출하여 사용한다.

함수의 오버로딩
같은이름의 함수를 만들수있다.
하지만 다른 매개변수를 받아야한다.